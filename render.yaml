services:
  - type: web
    name: gene-classifier
    env: python
    pythonVersion: "3.11"
    buildCommand: python -m pip install --upgrade pip setuptools wheel && pip install -r requirements.txt && python -m nltk.downloader stopwords wordnet punkt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
